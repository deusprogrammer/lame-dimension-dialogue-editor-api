let express,jsonwebtoken,authConfig,Users;_39d‍.w("express",[["default",["express"],function(v){express=v}]]);_39d‍.w("jsonwebtoken",[["default",["jsonwebtoken"],function(v){jsonwebtoken=v}]]);_39d‍.w("../config/authConfig",[["default",["authConfig"],function(v){authConfig=v}]]);_39d‍.w("../models/user",[["default",["Users"],function(v){Users=v}]]);




const router = express.Router();

router.post('/', async (req, res, next) => {
    const user = await Users.findOne({ username: req.body.username });
    if (!user || !user.isValidPassword(req.password)) {
        res.statusCode = 401;
        return res.send();
    }

    const body = {
        _id: user._id,
        username: user.username,
        roles: user.roles,
    };
    const jwtToken = jsonwebtoken.sign({ user: body }, authConfig.key);

    return res.json({
        jwtToken,
    });
});

module.exports = router;
